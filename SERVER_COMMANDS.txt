You're on the server terminal. Run these commands:

# 1. Check for CRITICAL ERROR logs (to see the 500 error details)
tail -100 /var/www/navigation2/app_error.log | grep -A20 "CRITICAL ERROR"

# 2. If nothing, check for recent errors
tail -50 /var/www/navigation2/app_error.log

# 3. Test the server
curl -k -s https://localhost:5001/google | head -5

# 4. Now go to browser and try to navigate, then come back and run:
tail -30 /var/www/navigation2/app_error.log

This will show the actual Python error causing the 500!


